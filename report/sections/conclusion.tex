\section{Conclusion}\label{sec:conclusion}
% Structure follows the research-paper-writing conclusion guide:
% 1. Restate the problem and the core idea (control flow in code, small structured calls).
% 2. Summarise the strongest evidence.
% 3. Practical implications for deploying Vietnamese drug QA on low-cost models.
% 4. Limitations paragraph, scope boundaries first:
%    - benchmark questions are rule-generated, so natural user questions may be harder;
%    - the fusion constant and the reranker instruction were selected on the same benchmark;
%    - the answer and judge models come from the same provider, and calibration measures
%      but cannot remove possible self-preference;
%    - reference answers were written by an LLM and not reviewed by a pharmacist;
%    - latency was measured on a development machine with a remote GPU reranker;
%    - leaflets come from a single online source.
% 5. Concrete future work (expert review, real user questions, more sources).
\pending{conclusion}
