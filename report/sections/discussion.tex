\section{Discussion}\label{sec:discussion}
% Outline (write once the end-to-end results are final):
% - RQ1: what the retrieval results imply for a deployable configuration.
% - RQ2: whether the agentic steps help low-cost models, per the ablations.
% - RQ3: safety behaviour and its uncertainty with ten items per case.
% Limitations to state explicitly:
% - Benchmark questions are rule-generated; natural user questions may be harder.
% - The fusion constant and the reranker instruction were selected on the same benchmark.
% - The answer and judge models come from the same provider; calibration measures, but
%   cannot remove, possible self-preference.
% - Reference answers were written by an LLM, and no pharmacist reviewed them.
% - Latency was measured on a development machine with a remote GPU reranker.
% - Leaflets come from a single online source.
\pending{discussion}
